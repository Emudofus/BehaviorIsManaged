<UserControl x:Class="BiM.Host.UI.Bot.GameUI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" DataContext="{Binding RelativeSource={RelativeSource Self}, Path=Bot.Character}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <TabControl>
            <TabItem Header="General"></TabItem>
            <TabItem Header="Inventory">
                <ListBox ItemsSource="{Binding Inventory.Items}">
                    
                </ListBox>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
