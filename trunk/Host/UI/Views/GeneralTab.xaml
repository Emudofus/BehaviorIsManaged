<UserControl x:Class="BiM.Host.UI.Views.GeneralTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:vm="clr-namespace:BiM.Host.UI.ViewModels" mc:Ignorable="d" 
             Height="300" Width="300"
             DataContext="{Binding vm:GeneralTabViewModel.ViewModel, RelativeSource={RelativeSource Mode=Self}}">
    <DockPanel>
    <RichTextBox DockPanel.Dock="Bottom" Name="RichTextBox">
      <RichTextBox.Resources>
        <Style TargetType="{x:Type Paragraph}">
          <Setter Property="Margin" Value="0" />
        </Style>
      </RichTextBox.Resources>
    </RichTextBox>
  </DockPanel>
</UserControl>
